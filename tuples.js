var stuff = [1, 'asd'];
var color = [23, 22, 21];
var color2 = [255, 0, 255]; //acts like a rigid array only three #
var goodRes = [200, 'great']; //rigid array.. must be in order
goodRes.push(123); //overflow, dont do this
goodRes.pop(); // 
goodRes.pop();
goodRes.pop(); //array is now empty. not following the rules
//alternate code
{
    code: 200;
    msg: 'great';
}
var responses = [[404, 'not found'], [200, "ok"],];
